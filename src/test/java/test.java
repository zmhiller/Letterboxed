public class test {

    /*
    Base class for assertion tests
    - inheritable methods for print output
    - child classes accept str input for test name,
        include assertion (w/ set-up as needed), and append
        PASS or FAIL as necessary.
    - boolean return could be useful (i.e. if(!assertion) becomes possible
     */
}
